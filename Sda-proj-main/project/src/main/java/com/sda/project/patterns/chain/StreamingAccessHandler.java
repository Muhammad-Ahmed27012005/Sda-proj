package com.sda.project.patterns.chain;

public class StreamingAccessHandler extends StreamingHandler {
    @Override
    public void handle(StreamingRequest request) {
        super.handle(request);
    }
}
